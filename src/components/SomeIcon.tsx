import styled from 'styled-components';

const StyledIcon = styled.div`
  font-size: 70%;
  width: 100px;
  height: 100px;
`;

const SomeIcon = () => (
  <StyledIcon>
    🐳
  </StyledIcon>
);

export default SomeIcon;
